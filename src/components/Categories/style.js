import styled from 'styled-components';

const CategoriesList = styled.section`
  display: flex;
  flex-direction: column;
  button {
    background: transparent;
    border: none;
    text-align: left;
  }
`;

export default {
  CategoriesList,
};
